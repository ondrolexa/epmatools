name: epmatools

dependencies:
  - python>=3.9
  # pandas and scientific python
  - click
  - numpy
  - matplotlib
  - scipy
  - h5py
  - pandas
  - scikit-learn
  - colorcet
  - xlrd
  - openpyxl
  - periodictable
  # Tools
  - jupyter
  - jupyterlab
  - jupyterlab-spellchecker
  - jupyterlab_code_formatter
  - pytest
  - isort
  - black
  - ipykernel
  - pip
  - pip:
    - -e . # install the local epmatools package

# mamba env create -f environment-dev.yml
# python -m ipykernel install --user --name epmatools

# mamba activate epmatools

# mamba deactivate
# mamba remove -n epmatools --all
# jupyter kernelspec uninstall epmatools
